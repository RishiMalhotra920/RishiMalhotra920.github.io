<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://rishimalhotra920.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://rishimalhotra920.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-11-30T02:44:39+00:00</updated><id>https://rishimalhotra920.github.io/feed.xml</id><title type="html">blank</title><subtitle>ML Engineer @ Apple working on transformer-based RL, LoRA training, and exploring diffusion models. </subtitle><entry><title type="html">Notes on Diffusion Models</title><link href="https://rishimalhotra920.github.io/blog/2025/notes-on-diffusion-models/" rel="alternate" type="text/html" title="Notes on Diffusion Models"/><published>2025-11-27T18:00:00+00:00</published><updated>2025-11-27T18:00:00+00:00</updated><id>https://rishimalhotra920.github.io/blog/2025/notes-on-diffusion-models</id><content type="html" xml:base="https://rishimalhotra920.github.io/blog/2025/notes-on-diffusion-models/"><![CDATA[<p>These notes are based heavily on [1].</p> <h2 id="basics">Basics</h2> <p>ODEs:</p> \[\frac{d}{dt} x_t = u_t(x_t) \tag{1}\] \[dx_t = u_t(x_t) dt \tag{2}\] <p>SDEs:</p> \[dx_t = u_t(x_t) dt + \sigma_t dW_t \tag{3}\] <p>Integrating the SDE:</p> \[\int_{0}^{t} dX_s = \int_{0}^t u_s(X_s) ds + \int_{0}^t \sigma_s dW_s \tag{4}\] <p>The first integral is our standard Riemann Integral:</p> \[\int_{0}^t u_s(X_s) ds = \lim_{n \to \infty} \sum_{i=0}^{n-1} u_{s_i}(X_{s_i}) \cdot (s_{i+1} - s_i) \tag{5}\] \[= \lim_{n \to \infty} \sum_{i=0}^{n-1} u_{s_i}(X_{s_i}) \cdot \frac{t}{n} \tag{6}\] <p>The second integral is an Itô integral, following Itô’s calculus:</p> \[\int_{0}^t \sigma_s dW_s = \lim_{n \to \infty} \sum_{i=0}^{n-1} \sigma_{s_i} \cdot (W_{s_{i+1}} - W_{s_i}) \tag{7}\] \[W_{s_{i+1}} - W_{s_i} \sim \mathcal{N}(0, s_{i+1} - s_i) \tag{8}\] <h2 id="forward-and-backward-sdes-and-odes">Forward and Backward SDEs and ODEs</h2> <p>Convention: $p_0$ is $p_{\text{init}}$ and $p_1$ is $p_{\text{data}}$.</p> <p>Forward process: add noise, convert $p_1 \to p_0$</p> <p>Backward process: remove noise, convert $p_0 \to p_1$</p> <p>Forward ODE:</p> \[\frac{d}{dt} X_t = u_t(X_t) \tag{9}\] <p>Backward ODE:</p> \[\frac{d}{dt} X_t = -u_t(X_t) \tag{10}\] <p>Forward SDE:</p> \[dX_t = u_t(X_t) dt + \sigma_t dW_t \tag{11}\] <p>Reverse SDE:</p> \[dX_t = \left[-u_t(X_t) + \sigma_t^2 \nabla \log p_t(X_t)\right] dt + \sigma_t dW_t \tag{12}\] <p>Note: you don’t simulate the forward ODE and the forward SDE. You just construct the noised object and run simulation on the backward ODE and backward SDE.</p> <h2 id="simulating-the-odes-and-sdes">Simulating the ODEs and SDEs</h2> <p><strong>Euler Method:</strong></p> <p>Running a Taylor series expansion of the ODE gives:</p> \[X_{t+\Delta t} = X_t + X'_t \Delta t + O(t^2) \tag{13}\] \[X_{t+\Delta t} = X_t + u_t^{\theta}(X_t) \Delta t + O(t^2) \tag{14}\] <p>Discretizing and keeping the 1st order term:</p> \[X_{t+h} = X_t + h u_t^{\theta}(X_t) \tag{15}\] <p><strong>Euler-Maruyama Method:</strong></p> <p>Starting from the integral form of the SDE:</p> \[\int_{t}^{t+\Delta t} dX_s = \int_{t}^{t+\Delta t} u_s(X_s) ds + \int_{t}^{t+\Delta t} \sigma_s dW_s \tag{16}\] \[X_{t+\Delta t} = X_t + \lim_{n \to \infty} \sum_{i=0}^{n-1} u_{s_i}(X_{s_i}) \cdot \frac{\Delta t}{n} + \lim_{n \to \infty} \sum_{i=0}^{n-1} \sigma_{s_i} \cdot (W_{s_{i+1}} - W_{s_i}) \tag{17}\] <p>where $s_i = t + i \cdot \frac{\Delta t}{n}$ and $s_{i+1} - s_i = \frac{\Delta t}{n}$.</p> <p>For a single step, this becomes:</p> \[X_{t+\Delta t} = X_t + u_t(X_t) \cdot \Delta t + \sigma_t \cdot (W_{t+\Delta t} - W_t) \tag{18}\] <p>Since $W_{t+\Delta t} - W_t \sim \mathcal{N}(0, \Delta t) = \sqrt{\Delta t} \cdot \mathcal{N}(0, 1)$, we can write:</p> \[X_{t+\Delta t} = X_t + u_t(X_t) \cdot \Delta t + \sigma_t \cdot \sqrt{\Delta t} \cdot \epsilon \tag{19}\] <p>where $\epsilon \sim \mathcal{N}(0, I)$.</p> <p>The following SDE follows the Fokker-Planck equation (see equation (30) below):</p> \[dX_t = \left[u_t^{\text{target}}(X_t) + \frac{\sigma_t^2}{2}\nabla \log p_t(X_t)\right] dt + \sigma_t dW_t \tag{20}\] <p>Discretizing using the Euler-Maruyama method:</p> \[X_{t+\Delta t} = X_t + u_t(X_t) \cdot \Delta t + \frac{\sigma_t^2}{2} \cdot s_t(X_t) \cdot \Delta t + \sigma_t \cdot \sqrt{\Delta t} \cdot \epsilon \tag{21}\] <p>where you can train networks for $u_t$ and $s_t$. In practice, if you use a Gaussian representation for $X_t$, you can write $s_t$ as a function of $u_t$ (see equation (52) below) and train just one network.</p> <p>The Euler method gives the ODE sampler. The Euler-Maruyama method produces the SDE sampler. Flow matching must use the ODE sampler; diffusion can use either the ODE or the SDE sampler!</p> <h2 id="training-networks-for-the-ode-and-sde">Training Networks for the ODE and SDE</h2> <p>I will consider the general case first, then assume the Gaussian case with linear noise schedules.</p> <p>We need to minimize:</p> \[\mathcal{L}(\theta) = \mathbb{E}_{t, x}\left[\|u_t^\theta(x) - u_t^{\text{target}}(x)\|^2\right] \tag{22}\] <p>$u_t^{\text{target}}(x)$ is actually not tractable. So, we can instead construct the following <strong>Conditional Flow Matching Loss</strong> by conditioning on $z$ (the real data):</p> \[\mathcal{L}_{\text{CFM}}(\theta) = \mathbb{E}_{t \sim \text{Unif}, z \sim p_{\text{data}}, x \sim p_t(\cdot | z)} \left[\|u_t^\theta(x) - u_t^{\text{target}}(x|z)\|^2\right] \tag{23}\] <p>For ODEs we must minimize the above loss. For SDEs, we can minimize the $\mathcal{L}<em>{\text{CFM}}(\theta)$ or the $\mathcal{L}</em>{\text{CSM}}$ loss since the score function can be written in terms of the velocity and vice versa.</p> \[\mathcal{L}_{\text{CSM}}(\theta) = \mathbb{E}_{t \sim \text{Unif}, z \sim p_{\text{data}}, x \sim p_t(\cdot | z)}\left[\|s_t^\theta(x) - \nabla \log p_t(x|z)\|^2\right] \tag{24}\] <p>The proof that $\mathcal{L}<em>{\text{CFM}}(\theta) = \mathcal{L}</em>{\text{FM}}(\theta) + C$ (where $C$ is independent of $\theta$) can be found in Theorem 18 in the MIT notes [1].</p> <p><strong>ODEs</strong></p> <p>We define a probability path as an object that defines the probability of being at $X$ at time $t$. This path also shows how the probability distribution evolves over time:</p> \[\{p_t(\cdot | z)\}_{t=0}^1 \tag{25}\] <p>Also, define a flow:</p> \[\begin{align} \psi_t(x_0) \quad &amp;\text{(flow)} \tag{26a}\\ \psi_0(x_0) &amp;= x_0 \quad \text{(initial condition)} \tag{26b}\\ \frac{d}{dt} \psi_t(x_0) &amp;= u_t(\psi_t(x_0)) \quad \text{(flow ODE)} \tag{26c} \end{align}\] <p>which defines the position $X_t$ reached as you simulate the ODE starting from $x_0$. $X_t$ is a random variable representing a point on just one trajectory. $\psi_t(x_0)$ is a function that represents position at time $t$ for a trajectory starting at $x_0$. It is defined for all positions.</p> <p>The flow ODE imposes a constraint that the function $\psi_t$ must follow. Note: since $\psi_t$ is deterministic, it is not defined for SDEs. Instead SDEs define $p_t(X_t)$, the probability of being at a position $X_t$ at time $t$.</p> <p>So, using the flow ODE for a Gaussian conditional probability path $p_t(\cdot|z) = \mathcal{N}(\alpha_t z, \beta_t^2 I)$:</p> \[\begin{align} \frac{d}{dt} \psi_t(x|z) &amp;= u_t^{\text{target}}(\psi_t(x|z) | z) \tag{27}\\ \frac{d}{dt} (\alpha_t z + \beta_t \epsilon) &amp;= u_t^{\text{target}}(\alpha_t z + \beta_t \epsilon | z) \tag{28}\\ \dot{\alpha}_t z + \dot{\beta}_t \epsilon &amp;= u_t^{\text{target}}(\alpha_t z + \beta_t \epsilon | z) \tag{29} \end{align}\] <p>Reparametrizing $x = \alpha_t z + \beta_t \epsilon$ and solving for $\epsilon = \frac{x - \alpha_t z}{\beta_t}$:</p> \[u_t^{\text{target}}(x|z) = \left(\dot{\alpha}_t - \frac{\dot{\beta}_t}{\beta_t}\alpha_t\right)z + \frac{\dot{\beta}_t}{\beta_t}x \tag{30}\] <p>With this, we have a way to pick any $x$, any $z$ and construct a training target for $u_t^{\text{target}}(x|z)$ and minimize the loss function.</p> <p>Some requirements on $\alpha_t$ and $\beta_t$:</p> <ol> <li>Continuously differentiable w.r.t. $t$</li> <li>Monotonic (for smooth progression from noise $\to$ data)</li> <li>Boundary conditions:</li> </ol> \[\begin{align} \alpha_0 &amp;= 0 = \beta_1 \tag{31a}\\ \alpha_1 &amp;= 1 = \beta_0 \tag{31b} \end{align}\] <p>When $\alpha_t = t$ and $\beta_t = 1-t$ with the Gaussian conditional probability path, we have:</p> \[u_t^{\text{target}}(x|z) = z - \epsilon \tag{32}\] <p>You can verify by substituting $\alpha_t = t$, $\beta_t = 1-t$, $\dot{\alpha}_t = 1$, and $\dot{\beta}_t = -1$ into equation (30).</p> <p>This simplifies the Conditional Flow Matching loss for Gaussian paths with this noise schedule. Many SOTA models are trained this way including SD3 and Meta Movie Gen Video:</p> \[\mathcal{L}_{\text{CFM}}(\theta) = \mathbb{E}_{t \sim \text{Unif}, z \sim p_{\text{data}}, \epsilon \sim \mathcal{N}(0, I)} \left[\|u_t^\theta(tz+(1-t)\epsilon) - (z-\epsilon)\|^2\right] \tag{33}\] <p>Notice that the target is a vector pointing from $\epsilon$ to $z$ and the input to the neural network is an interpolation between $z$ and $\epsilon$ based on $t$.</p> <p><strong>Algorithm 1: Flow Matching Training (Gaussian CondOT path)</strong></p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>for each mini-batch of data:
    Sample z from the dataset
    Sample t ~ Unif[0, 1]
    Sample ε ~ N(0, I)
    Set x = tz + (1-t)ε
    Compute loss: L(θ) = ||u_t^θ(x) - (z - ε)||^2
    Update θ via gradient descent
</code></pre></div></div> <h3 id="sdes">SDEs</h3> <p>We need to construct an SDE that follows the Fokker-Planck equation so that $X_t$ follows a probability path $p_t$.</p> <p><strong>The Fokker-Planck Equation:</strong></p> <p>Consider the SDE:</p> \[X_0 \sim p_{\text{init}}, \quad dX_t = u_t(X_t) dt + \sigma_t dW_t \tag{34}\] <p>Then $X_t$ has distribution $p_t$ for all $0 \leq t \leq 1$ if and only if the Fokker-Planck equation holds:</p> \[\begin{align} \frac{\partial p_t(x)}{\partial t} &amp;= -\text{div}(p_t u_t)(x) + \frac{\sigma_t^2}{2} \Delta p_t(x) \quad \text{for all } x \in \mathbb{R}^d, 0 \leq t \leq 1 \tag{35}\\ \text{div}(v_t)(x) &amp;= \sum_{i=1}^{d} \frac{\partial}{\partial x_i} v_t(x) \quad \text{(Divergence)} \tag{36}\\ \Delta p_t(x) &amp;= \sum_{i=1}^{d} \frac{\partial^2}{\partial x_i^2} p_t(x) \quad \text{(Laplacian)} \tag{37} \end{align}\] <p>Intuitively, the divergence denotes how much flow is moving out of a unit volume over time. The Laplacian denotes the curvature of the probability distribution, e.g., if Laplacian $&gt; 0$, probability will accumulate in that region.</p> <p>Imagine particles moving over a certain location $x$ over time according to the drift term, plus some noise is added at each timestep due to the diffusion term in the SDE. Then, intuitively, the Fokker-Planck equation shows that the change in probability at a location $x$ over time $t$ is the sum of the inflow to the unit volume (drift term) plus how the noise would accumulate at that location (diffusion term).</p> <p>Proof for this is present in Appendix B of the MIT notes.</p> <p>Then, the SDE below follows the Fokker-Planck equation. The term in square brackets is the realization for $u_t(X_t)$ in the general SDE equation:</p> \[dX_t = \left[u_t^{\text{target}}(X_t) + \frac{\sigma_t^2}{2}\nabla \log p_t(X_t)\right] dt + \sigma_t dW_t \tag{38}\] <p>which implies:</p> \[X_t \sim p_t \tag{39}\] <p>This is very similar to the ODE case:</p> \[\begin{align} \frac{d}{dt} X_t &amp;= u_t^{\theta}(X_t) \tag{40}\\ X_t &amp;\sim p_t \tag{41} \end{align}\] <p>Specifically, Theorem 13 in the MIT notes says that if you construct an SDE with the form in equation (38), then $X_t$ follows the same distribution as in the much simpler ODE case.</p> <p>With that, it becomes easy to compute $u_t^{\text{target}}(X_t|z)$ and $s_t^{\text{target}}(X_t|z)$ to estimate $u_t(X_t|z)$ and $\nabla \log p_t(X_t|z)$.</p> <p>$u_t^{\text{target}}$ is the same as before (equation (30)) and for $s_t^{\text{target}}(x|z)$, you take the gradient of the log of the Gaussian PDF:</p> \[\begin{align} u_t^{\text{target}}(x|z) &amp;= \left(\dot{\alpha}_t - \frac{\dot{\beta}_t}{\beta_t}\alpha_t\right)z + \frac{\dot{\beta}_t}{\beta_t}x \tag{42}\\ s_t^{\text{target}}(x|z) &amp;= \nabla \log p_t(x|z) = -\frac{x-\alpha_t z}{\beta_t^2} \tag{43} \end{align}\] <p>Minimizing the $s_t^{\theta}$ term takes us to:</p> \[\begin{align} \mathcal{L}_{\text{CSM}}(\theta) &amp;= \mathbb{E}\left[\left\|s_t^{\theta}(x) - s_t^{\text{target}}(x|z)\right\|^2\right] \tag{44}\\ &amp;= \mathbb{E}\left[\left\|s_t^{\theta}(x) + \frac{x-\alpha_t z}{\beta_t^2}\right\|^2\right] \tag{45}\\ &amp;= \mathbb{E}\left[\frac{1}{\beta_t^2}\left\|\beta_t s_t^{\theta}(\alpha_t z + \beta_t \epsilon) + \epsilon\right\|^2\right] \tag{46} \end{align}\] <p>We reparametrize $\epsilon_t^{\theta}$ to now estimate $-\beta_t s_t^{\theta}(x)$ and drop the $\frac{1}{\beta_t^2}$ factor because DDPM showed that with $\beta_t$ in the denominator the loss was unstable. This gives:</p> \[\mathcal{L}_{\text{DDPM}}(\theta) \approx \mathbb{E}_{t \sim \text{Unif}, z \sim p_{\text{data}}, \epsilon \sim \mathcal{N}(0,I)}\left[\left\| \epsilon_t(\alpha_t z + \beta_t \epsilon) - \epsilon\right\|^2\right] \tag{47}\] <p><strong>Algorithm 2: Score Matching Training (Denoising Diffusion)</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>for each mini-batch of data:
    Sample z from the dataset
    Sample t ~ Unif[0, 1]
    Sample ε ~ N(0, I)
    Set x = α_t z + β_t ε
    Compute loss: L(θ) = ||ε_t^θ(x) - ε||^2
    Update θ via gradient descent
</code></pre></div></div> <p>where the network $\epsilon_t^{\theta}$ now estimates the noise.</p> <p>Once you have learned $\epsilon_t^{\theta}$, which is related to $s_t^{\theta}$ by $\epsilon_t^{\theta}(x) = -\beta_t s_t^{\theta}(x)$, you can use the following conversion formula to find $u_t^{\theta}$:</p> \[u_t^{\theta}(x) = \beta_t^2 \left(\frac{\dot{\alpha}_t}{\alpha_t} - \frac{\dot{\beta}_t}{\beta_t}\right) s_t^{\theta}(x) + \frac{\dot{\alpha}_t}{\alpha_t} x \tag{48}\] <p>or equivalently in terms of the noise predictor:</p> \[u_t^{\theta}(x) = -\beta_t \left(\frac{\dot{\alpha}_t}{\alpha_t} - \frac{\dot{\beta}_t}{\beta_t}\right) \epsilon_t^{\theta}(x) + \frac{\dot{\alpha}_t}{\alpha_t} x \tag{49}\] <p>This is derived from Proposition 1 in the MIT notes.</p> <p>Alternatively, you can also minimize $u_t^{\theta}$ directly using Algorithm 1 we discussed above.</p> <p>Then use the Euler-Maruyama method (equation (21)) to generate samples.</p> <p>Here is the SDE following Fokker-Planck in terms of $s_t^{\theta}$:</p> \[dX_t = \left[u_t^{\theta}(X_t) + \frac{\sigma_t^2}{2}s_t^{\theta}(X_t)\right] dt + \sigma_t dW_t \tag{50}\] <p>The conversion between score and velocity for Gaussian paths:</p> \[s_t^{\theta}(x) = \frac{\alpha_t u_t^{\theta}(x) - \dot{\alpha}_t x}{\beta_t^2 \dot{\alpha}_t - \alpha_t \dot{\beta}_t \beta_t} \tag{52}\] <p>Note, you can train $u_t^{\theta}$ or $s_t^{\theta}$ and then derive the other through this analytical form.</p> <h2 id="conditioning-on-images">Conditioning on Images</h2> <p>Same as above, but now we want to minimize:</p> <h3 id="odes">ODEs</h3> \[\mathcal{L}_{\text{CFM}}^{\text{guided}}(\theta) = \mathbb{E}_{(z,y) \sim p_{\text{data}}, t \sim \text{Unif}, x \sim p_t(\cdot|z)}\left[\|u_t^{\theta}(x | y) - u_t^{\text{target}}(x | z)\|^2\right] \tag{53}\] <p>where $(y, z) \sim p_{\text{data}}$ are sampled jointly (e.g., text prompt and image).</p> <p>However, it was found that during inference, images generated don’t adhere well enough to the prompt. So you train $u_t$ normally but during inference you scale up the contribution from the guidance term.</p> <p>The guided vector field can be decomposed as:</p> \[u_t^{\text{target}}(x|y) = u_t^{\text{target}}(x) + b_t \nabla \log p_t(y|x) \tag{54}\] <p>where $b_t = \beta_t^2 \left(\frac{\dot{\alpha}_t}{\alpha_t} - \frac{\dot{\beta}_t}{\beta_t}\right)$.</p> <p>You can view the first term as the natural flow and the second term as a guidance signal that shows how the log probability of $y|x$ changes w.r.t. $x$. So, as the pixels of blurry image $x$ change, how does the probability of text $y$ change. Note the sum of these two terms forms the target which is the true signal your network $u_t^{\theta}$ must approximate.</p> <p>So, to increase your network’s adherence to the text signal during inference, you add a weight $w &gt; 1$:</p> \[\tilde{u}_t(x|y) = u_t^{\text{target}}(x) + w \cdot b_t \nabla \log p_t(y|x) \tag{55}\] <p>which can be rewritten as:</p> \[\tilde{u}_t(x|y) = (1-w)u_t^{\text{target}}(x|\emptyset) + w \cdot u_t^{\text{target}}(x|y) \tag{56}\] <p>where $\emptyset$ denotes an empty/null conditioning token.</p> <p><strong>Classifier-Free Guidance Training:</strong></p> <p>When training, you replace the label $y$ with an empty token $\emptyset$ with probability $\eta \in [0,1]$ (typically $\eta = 0.1$). This trains the model to approximate both $u_t^{\text{target}}(x|\emptyset)$ (unconditional) and $u_t^{\text{target}}(x|y)$ (conditional) in a single network.</p> <p><strong>Algorithm 3: Classifier-Free Guidance Training</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>for each mini-batch of data:
    Sample (z, y) from the dataset
    Sample t ~ Unif[0, 1]
    Sample ε ~ N(0, I)
    Set x = α_t z + β_t ε

    With probability η:
        Set y = ∅  (empty token)

    Compute loss: L(θ) = ||u_t^θ(x, y, t) - (z - ε)||^2
    Update θ via gradient descent
</code></pre></div></div> <p>At inference time, use equation (56) with $w &gt; 1$ (typically $w \in [1.5, 7.5]$).</p> <h2 id="sdes-1">SDEs</h2> <p>You follow the same process as for ODEs when training the $u$ function and the $s$ function, but need to drop the label $y$ with probability $\eta$ as discussed above.</p> <p>However, during inference, you need to amplify the probability $w$ for both the score function and the velocity function.</p> <p>For the score function:</p> \[\tilde{s}_t^{\theta}(x|y) = (1-w) \cdot s_t^{\theta}(x|\emptyset) + w \cdot s_t^{\theta}(x|y) \tag{57}\] <p>or equivalently:</p> \[\tilde{s}_t^{\theta}(x|y) = (1-w) \cdot \nabla \log p_t(x|\emptyset) + w \cdot \nabla \log p_t(x|y) \tag{58}\] <p>Then the SDE for sampling becomes:</p> \[dX_t = \left[u_t^{\theta}(X_t, y, t) + \frac{\sigma_t^2}{2}\tilde{s}_t^{\theta}(X_t|y)\right] dt + \sigma_t dW_t \tag{59}\] <h3 id="architectures">Architectures</h3> <p><strong>U-Net:</strong> Consists of encoders (increase channels, reduce spatial dimensions), midcoders (latent processing blocks), and decoders (reduce channels, increase spatial dimensions). Skip connections connect encoder and decoder layers at the same resolution.</p> <p><strong>DiT (Diffusion Transformer):</strong> Uses transformer blocks instead of convolutional layers, treating the image as a sequence of patches.</p> <h3 id="encoding-the-guiding-variable">Encoding the Guiding Variable</h3> <p><strong>When $y_{\text{raw}}$ is a class label:</strong> Initialize and add an embedding network to $\theta$ and train the embedding for $y_{\text{raw}}$ end-to-end.</p> <p><strong>When $y_{\text{raw}}$ is text:</strong> Use CLIP or SigLIP embeddings or combine multiple such embeddings (e.g., CLIP for visual grounding, T5 for language understanding).</p> <p><strong>Feeding in the embedding:</strong></p> <ul> <li>Feed it into every sub-component of architecture for images. E.g., in U-Net, $y_{\text{embed}}$ and $t$ are passed to each encoder, midcoder, and decoder block.</li> <li>You can project $y$ to a shape of $C \times 1 \times 1$ and add it to each pixel in $x \in \mathbb{R}^{C \times H \times W}$.</li> <li>Alternatively, you can use cross-attention between image features and text embeddings.</li> </ul> <h3 id="stable-diffusion-3">Stable Diffusion 3</h3> <p>For high-resolution images, you should work in the latent space of that image. SD3 trained an autoencoder (VAE) to map images to latent space and then runs flow matching in the diffusion process within this latent space.</p> <p>They use:</p> <ul> <li><strong>CLIP embeddings</strong> for visual grounding in images</li> <li><strong>T5 embeddings</strong> for high-level understanding of language</li> </ul> <p>This multi-embedding approach allows the model to leverage both visual concepts and complex language understanding.</p> <h3 id="meta-movie-gen-video">Meta Movie Gen Video</h3> <p>Introduced a <strong>temporal autoencoder</strong> mapping a raw video to a latent representation. The video is chopped up into spatiotemporal cubes $\to$ encoded $\to$ encodings are stitched together. The U-Net (or transformer) now predicts the velocity field or the score function in this latent space.</p> <p>Key innovations:</p> <ul> <li>Temporal compression in addition to spatial compression</li> <li>Joint training on images and videos</li> <li>Scalable architecture that can handle variable-length videos</li> </ul> <hr/> <p>Written and organized by Rishi. Cleanup by Claude Sonnet.</p> <h2 id="references">References</h2> <p>[1] P. Holderrieth and E. Erives, “An Introduction to Flow Matching and Diffusion Models,” MIT Class 6.S184, 2025. Available: <a href="https://arxiv.org/pdf/2506.02070">https://arxiv.org/pdf/2506.02070</a></p>]]></content><author><name></name></author><category term="ml-research"/><category term="diffusion-models"/><category term="math"/><summary type="html"><![CDATA[Some notes on diffusion models starting with mathematical foundations.]]></summary></entry></feed>